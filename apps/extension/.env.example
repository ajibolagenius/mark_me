# URL of the deployed mark_me web app (used for all API calls)
VITE_API_URL=http://localhost:3000

# OAuth callback URL for chrome.identity flows (set to your extension's OAuth callback)
# Format: https://<extension-id>.chromiumapp.org/oauth
VITE_OAUTH_CALLBACK=
